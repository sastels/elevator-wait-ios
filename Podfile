# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'elevator-wait' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for elevator-wait

  # add the Firebase pod for Google Analytics
  pod 'Firebase/Analytics'
  pod 'Firebase/Firestore'
  pod 'FirebaseFirestoreSwift'

  # add pods for any other desired Firebase products
  # https://firebase.google.com/docs/ios/setup#available-pods
  
  target 'elevator-waitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'elevator-waitUITests' do
    # Pods for testing
  end

end

target 'elevator-wait WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for elevator-wait WatchKit App

end

target 'elevator-wait WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for elevator-wait WatchKit Extension

end
